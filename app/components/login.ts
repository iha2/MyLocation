// the point of making this was to show and understanding of interfaces
// but also something I would do in case there are other login's that we
// need like twitter, linkedin e.t.c
export interface Login {
    loginUser(): void
}